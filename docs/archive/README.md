# Historical material

These files are preserved for provenance and are not active runtime instructions.

- `planning-procedure/`: the retired repo-local Markdown skill, now ordinary docs.
- `planning-tools/`: its Python state helper and tests, retained together.
- `python-draft/`: the pre-existing alternative Python draft and architecture,
  preserved without adopting its conflicting policies or unfinished components.

Use the [current README](../../README.md) and Java implementation. In particular,
old proposal-only limitations, schedules, caps or approval flows are not current
product requirements unless explicitly retained in `PLANNING.md`.
